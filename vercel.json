{
  "buildCommand": "prisma generate && prisma db push && next build",
  "functions": {
    "src/app/api/**": {
      "maxDuration": 60
    }
  },
  "crons": [
    {
      "path": "/api/cron/daily",
      "schedule": "0 22 * * *"
    }
  ]
}
